export default function SettingsPage() {
  return (
    <div className="glass-card" style={{ height: '100%', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
      <h1 className="text-glow" style={{ fontSize: '2rem', marginBottom: '1rem' }}>Settings</h1>
      <p style={{ color: 'var(--text-secondary)' }}>User preferences and system configuration pending...</p>
    </div>
  );
}
